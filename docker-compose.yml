services:
  db:
    image: timescale/timescaledb:latest-pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cems_dev
    ports:
      - "5432:5432"
    volumes:
      - cems_db_data:/var/lib/postgresql/data

volumes:
  cems_db_data:
