DATABASE_URL=postgres://postgres:postgres@localhost:5432/cems_dev

# Only needed to run the real Wendeware live connector (src/connectors/wendeware/live-ingest.service.ts)
# against an actual myPowerGrid account. Never commit real values. A connector's secret_reference
# in the DB should then be the literal string "env:MPG_CLIENT_ID,env:MPG_CLIENT_SECRET".
MPG_CLIENT_ID=
MPG_CLIENT_SECRET=
